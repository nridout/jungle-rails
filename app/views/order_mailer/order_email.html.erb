<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
    <style>
      table, th, td {
        border: 1px solid lightgrey;
        border-collapse: collapse;
      }
      th, td {
        padding: 10px;
        text-align: left;
      }
      </style>
  </head>
  <body>
    <h3>Thank you for your order, <%= @order.email %>!</h3>
    <h4>Here are the details of your order:</h4>

    <div>
      <table>
        <thead>
          <tr>
            <th>Qty.</th>
            <th>Product</th>
            <th>Price</th>
          </tr>
        </thead>
        <tbody>
          <% @line_items.each do |line_item| %>
            <tr>
              <td>
                <%= line_item.quantity %>
              </td>
              <td>
                <%= line_item.product.name %>
              </td>
              <td>
                $<%= line_item.product.price * line_item.quantity %>
              </td>
            </tr>
          <% end %>
        </tbody>
        <tfoot>
          <tr>
            <th colspan="2">
              TOTAL:
            </th>
            <th>
              $<%= @order.total_cents / 100.0 %>
            </th>
          </tr>
        </tfoot>
      </table>
    </div>

    <p>Have a great day!</p>
  </body>
</html>