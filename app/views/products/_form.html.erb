<%= form_for :review, url: "#{@product.id}/reviews" do |f| %>

  <%= f.label :rating %>
  <%= f.select :rating, (1..5) %>
  <br>
  <%= f.label :description %>
  <%= f.text_field :description, :rows=>10, class: 'form-control' %>

  <%= f.submit "Submit", class: "btn btn-primary" %>

<% end %>