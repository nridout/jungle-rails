<%= form_for :review, url: '/products/:product_id/reviews' do |f| %>

  <%= f.label :rating %>
  <%= f.select :rating, (1..5) %>

  <%= f.label :description %>
  <%= f.text_field :description, class: 'form-control' %>

  <%= f.submit "Submit", class: "btn btn-primary" %>

<% end %>